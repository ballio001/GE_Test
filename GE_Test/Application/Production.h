#include <vector>
#include <iostream>
#include <algorithm>

class Production {
public:
	int testSize(std::vector<int> vector);
	int testNumber(int number);
	int testDuplicate(std::vector<int> vector);
};